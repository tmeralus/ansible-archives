[Unit]
Description=Redis In-Memory Data Store
After=network.target

[Service]
Type=notify
ExecStart=/usr/local/bin/redis-server /etc/redis/redis.conf
ExecStop=/usr/local/bin/redis-cli shutdown redis
Restart=always
RestartSec=10
LimitNOFILE=10240

[Install]
WantedBy=multi-user.target
